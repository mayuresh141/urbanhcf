name: Keep Render Alive

on:
  schedule:
    - cron: "*/13 * * * *"   # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: |
          curl -m 13 https://urbanhcf.onrender.com/health
